{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "bash start.sh",
        "releaseCommand": "touch database/database.sqlite && php artisan migrate --force && php artisan db:seed --class=DemoSeeder --force",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
